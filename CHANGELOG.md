# Changelog

このファイルには、Docfillyの利用者に影響する変更を記録します。

## Unreleased

### Added

- コアAPIと`@docfilly/react`に`initialValues`を追加し、保存済みのフォーム値をソースを書き換えずに初期表示へ適用できるようにしました。

### Fixed

- チェックボックス構文の`[X]`を、大文字小文字を区別せず`[x]`と同じ初期状態ONとして扱うようにしました。従来`[X]`を初期状態OFFとして利用していた文書では挙動が変わります。初期状態OFFには`[ ]`を使用してください。
